name: Scheduled Echo

on:
  schedule:
    - cron: '*/10 * * * *'  # 10分ごとに実行

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo a message
        run: echo "これは10分ごとに表示されるメッセージです。"
