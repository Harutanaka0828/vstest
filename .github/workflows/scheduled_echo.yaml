name: Scheduled Echo

on:
  push:  # 手動でpushすることで実行される
  schedule:
    - cron: '*/15 * * * *'  # 15分ごとに実行

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo a message
        run: echo "これは15分ごとに表示されるメッセージです。"
